# Generated at build/publish time from SKILL.md + references/
dist/

node_modules/
npm-debug.log*
*.tgz
.DS_Store

# Lockfile written when this package installs into a project
.dopod-design.lock.json

# Operator runbooks now live in the private repo realus99/dopod-design-admin.
# Kept ignored so a local admin-docs/ can never be committed here by accident.
admin-docs/
