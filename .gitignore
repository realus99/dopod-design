# Generated at build/publish time from SKILL.md + references/
dist/

node_modules/
npm-debug.log*
*.tgz
.DS_Store

# Lockfile written when this package installs into a project
.dopod-design.lock.json

# Local-only operator runbooks. Never pushed — see admin-docs/README.md for why
# and for how to share them with the team.
admin-docs/
